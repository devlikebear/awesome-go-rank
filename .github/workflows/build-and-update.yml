# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Build and Update

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: write
  pull-requests: read

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'

    - name: Run tests with coverage
      run: |
        go test -v -race -coverprofile=coverage.out -covermode=atomic ./...

    - name: Display coverage summary
      run: |
        go tool cover -func=coverage.out

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.out
        fail_ci_if_error: false
        verbose: true

    - name: Build
      run: |
        go mod tidy
        chmod u+x ./build.sh
        ./build.sh

    # - name: Run awesome-go-rank
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.API_TOKEN }}
    #   run: |
    #     chmod u+x ./bin/linux/amd64/awesome-go-rank
    #     ./bin/linux/amd64/awesome-go-rank

    # - name: Copy JSON to web directory
    #   run: |
    #     mkdir -p web/public/data
    #     cp public/data/repos.json web/public/data/repos.json

    - name: Commit and push changes
      uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: Build & Update ranking
        commit_user_name: GitHub Actions
        commit_user_email: actions@github.com
   